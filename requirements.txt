# Raspberry Pi GrowMate V2 Pods - Python Dependencies

# Async framework + HTTP
aiohttp>=3.8.0
APScheduler>=3.10.0

# GPIO control
gpiozero>=1.6.2
RPi.GPIO>=0.7.1

# ADC support (ADS1115)
adafruit-circuitpython-ads1x15>=2.2.21
adafruit-blinka>=8.20.0

# DHT22 temperature/humidity sensor
adafruit-circuitpython-dht>=3.7.9

# Web server for AP mode onboarding portal
flask>=2.3.0
werkzeug>=2.3.0

# Configuration management (YAML + validation + hot-reload)
pyyaml>=6.0
pydantic>=2.0.0
watchdog>=3.0.0

# System utilities
psutil>=5.9.0

# Logging (JSON to journald)
python-systemd>=234
python-json-logger>=2.0.0
